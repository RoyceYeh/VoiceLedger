請執行以下收尾流程：

1. **整理本次對話重點**，包含：
   - 實作了什麼功能
   - 關鍵技術決策與原因
   - 踩過的坑或注意事項
   - 未完成的部分

2. **更新對應文件**：
   - 根據內容判斷應寫入 `.docs/` 下哪個檔案
   - 若沒有對應檔案則新建一個
   - 檔名加上日期，例如：`## 2026-04-01`
   - 用 append 方式加入，不要覆蓋舊內容
   - 每筆記錄前加上日期，例如：`## 2026-04-01`

3. **更新 `.docs/todos.md`**：
   - 把本次完成的項目標為 `[x]`
   - 把提到的待辦加入 `[ ]`

4. **強制更新 CLAUDE.md 索引**：
   - 讀取目前 `.docs/` 下所有檔案
   - 與 CLAUDE.md 的索引表對比
   - 有新增的檔案一定要加進索引表
   - 已刪除的檔案從索引移除
   - 這步驟不可跳過

5. 最後輸出收尾摘要：
   - 本次更新了哪些文件
   - 新增了哪些文件（若有）
   - CLAUDE.md 索引是否有變動
   - 「✅ 收尾完成，可以開新 session 了」

```

---

## 三、日常開發流程
```

開新 session
↓ Claude 自動讀 CLAUDE.md → 讀 todos.md 了解現況
↓ 開始開發
↓ context 到 70% → ⚠️ 自動提醒
↓ context 到 85% → ⛔ 強烈警告
↓ 輸入 /wrap-up
↓ Claude 自動整理並更新所有文件
↓ 開新 session 繼續
